# 会議日程調整ツール 動作確認手順

Google スプレッドシートにスクリプトをデプロイしたあと、以下の手順で動作を確認してください。

## 前提

- 新規の Google スプレッドシートを作成していること
- 拡張機能 > Apps Script で本リポジトリの .gs ファイル（Code.gs, Config.gs, CalendarSync.gs, SlotFinder.gs, FormHandler.gs, Notification.gs, Utils.gs, Setup.gs）をすべてコピーしてプロジェクトに配置していること
- 初回実行時に表示される OAuth 認可で、Calendar・Gmail・Spreadsheet のスコープを許可していること

---

## 確認チェックリスト

| # | 確認項目 | 手順 |
|---|----------|------|
| 1 | **メニュー表示** | スプレッドシートを開く → 「📅 会議調整ツール」メニューが表示されること |
| 2 | **初期セットアップ** | メニュー「初期セットアップ」を実行 → 設定・参加者マスタ・カレンダー同期・フォーム回答・空き時間算出・候補日時の 6 シートが作成され、ヘッダと設定の初期値が入ること |
| 3 | **設定・参加者入力** | 「設定」シートで検索開始日・終了日を確認する。「参加者マスタ」にテスト用メール（少なくとも1件）を入力する |
| 4 | **カレンダー同期** | メニュー「カレンダー同期（全参加者）」を実行 → 「カレンダー同期」シートに列 A〜H でデータが入る（参加者が0人または権限が無い場合は空でもエラーにならないこと）。権限エラー時は設計書 3.1 の「権限とアクセス制御」を確認する |
| 5 | **トリガー設定** | メニュー「トリガー設定」を実行 → スクリプトエディタの「トリガー」に「syncAllCalendars（毎時）」「onFormSubmit（フォーム送信時）」が登録されること |
| 6 | **フォーム連携** | Google フォームを作成し、「回答」タブから「スプレッドシートにリンク」で本スプレッドシートの「フォーム回答」シートにリンクする。フォーム項目を設計書 3.2 に合わせる（会議タイトル、主催者メール、参加者、希望開始日・終了日、所要時間、希望時間帯開始・終了、優先条件、備考）。「設定」シートの「フォームURL」行にフォームのURLを入力する |
| 7 | **フォーム送信〜結果** | フォームで会議タイトル・主催者メール・参加者・希望日範囲・所要時間・時間帯等を送信する → 「フォーム回答」シートの処理ステータス列（L列）に「処理完了」が書き込まれ、空き時間算出・候補日時シートに結果が出力されること |
| 8 | **メール通知** | 上記送信後、主催者メールアドレスに「候補日時 TOP5」の通知メールが届くこと |

---

## 補足

- **空き時間を再算出**: メニュー「空き時間を再算出」で、最新のフォーム回答をもとに空き時間のみ再計算し、結果シートを上書きします（メールは送信されません）。
- **確定した会議をカレンダーに登録**: 「候補日時」シートで「決定」列にチェックを入れた行の日付・時間帯で、デフォルトカレンダーにイベントを作成します。会議タイトル・参加者は最新のフォーム回答から取得します。
- **フォームを開く**: 「設定」シートの「フォームURL」にフォームのURLを入力しておくと、メニュー「フォームを開く」でリンクを表示できます。
